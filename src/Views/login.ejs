<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="../css/login.css">
</head>
<body>
    <!-- <div class="login-container">
        <div class="login-box">
            <h2>Iniciar sesión</h2>
            <form id="form-login">
                <div class="input-group">
                    <label for="email">Email</label>
                    <input type="text" id="email" name="email" >
                </div>
                <div class="input-group">
                    <label for="password">Contraseña</label>
                    <input type="password" id="password" name="password" >
                </div>
                <button type="submit" class="login-btn">Ingresar</button>
                <h1 id="message"></h1>
                <div class="forgot-password">
                    <a href="#">¿Olvidaste tu contraseña?</a>
                    <a href="/auth/sign-up">¿Aún no estás registrado?</a>
                    <a href="/post/postlist">Solo vengo de visita</a>
                </div>
            </form>
        </div>
    </div> -->

    <div class="login-container v1">
        <h2>Ingresar a Literatura Perenne</h2>
        <form id="form-login">
            <div class="input-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" required>
            </div>
            <div class="input-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit" class="login-btn">Ingresar</button>
            <h1 id="message"></h1>
            <div class="additional-options">
                <!-- <a href="#">Forgot your scroll?</a> -->
                <a href="/auth/sign-up">Registrate si no lo estás</a>
                <a href="/post/postlist">Solo vengo de visita</a>
            </div>
        </form>
    </div>
</body>
</html>
<script>
    const formLogin = document.getElementById("form-login")
    const email = document.getElementById("email")
    const password = document.getElementById("password")
    const message = document.getElementById("message")

    formLogin.addEventListener("submit", (event)=>{
        event.preventDefault()
        fetch("/auth/login",{
            method:"POST",
            headers:{
                'Content-Type': 'application/json'
            },
            body:JSON.stringify({email:email.value, password:password.value})
        })
        .then(response =>response.json())
        .then(data=>{
            if(!data.success){
                message.innerHTML = data.message
            }
            else{
                console.log("Credenciales correctas")
                window.location.href = "/post/partialAllPost";
            }
        })
    })
</script>
